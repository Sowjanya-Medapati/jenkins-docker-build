pipeline {
    agent {
     docker {
      image 'maven:3.6.3-jdk-8'
      }
     }

    stages {
        stage('build') {
            steps {
                echo "Hello world !!"
                echo "Hello Sowjanya !!!!"
                sh 'mvn -B -DskipTests clean package'
                echo "done"
            }
        }
    }
}
