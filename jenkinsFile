pipeline {
    agent {
     docker {
      image 'maven:3.8.3-jdk-11-slim'
      }
     }

    stages {
        stage('build') {
            steps {
                echo "Hello world !!"
                echo "Hello Sowjanya !!!!"
                sh 'mvn -B -DskipTests clean package'
                echo "done"
            }
        }
    }
}
